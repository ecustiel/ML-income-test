/* const express = require('express');
const search = require('../controllers/search');

const api = express.Router();

//api.get('/', search.search);
api.get('', search.searchTest );

module.exports = api; */